{
    "Database": {
        "ConnectionString": "Server={server};Database={db};Port={port};User Id={username};Password={password};Ssl Mode=Require;",
        "CommandTimeout": 120,
        "Environment": "DEV",
        "Version": "0"
    }
}
